# MongoDB Configuration
#
# Configure your MongoDB connection settings below.
# Copy this file to .env and update with your actual values.
#
# MongoDB can be:
# - Local: Use localhost (requires MongoDB installed locally)
# - Network: Use your server's IP address (e.g., Unraid, NAS)
# - Cloud: Use MongoDB Atlas connection string
#

# MongoDB Server IP or Hostname
# Examples:
#   localhost (for local MongoDB)
#   192.168.1.27 (for network server like Unraid)
MONGO_HOST=localhost

# MongoDB Port (default: 27017)
MONGO_PORT=27017

# MongoDB Username
# Use the username you created when setting up MongoDB
MONGO_USERNAME=admin

# MongoDB Password
# Use the password for your MongoDB user
MONGO_PASSWORD=your_password_here

# Database Name
# This database will be created automatically if it doesn't exist
MONGO_DATABASE=dashboard

# Backend Server Configuration
# Port the Express backend server will run on
BACKEND_PORT=3001

# Admin Authentication
# Secret key for JWT token signing (IMPORTANT: Change this in production!)
# Generate a random string for production use
JWT_SECRET=your-secret-key-change-in-production
